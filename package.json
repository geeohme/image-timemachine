{
  "name": "gemini-image-time-machine",
  "version": "1.0.0",
  "description": "An application that uses Gemini to analyze, edit, and transform user-uploaded photos into historical scenes.",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --loader:.tsx=tsx --define:process.env.NODE_ENV='\"production\"' && cp index.html dist/index.html",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
